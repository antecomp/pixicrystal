# This is a comment
@start
Hello, friend. <face:smile>
How are you? {
    ?: Doing well!
        @doingwell
        That is great to hear. <face:pleased>
    ?: Doing bad.
        Sorry to hear that.
        -> elsewhere
    ?: Meh.
        # edge case, goto directly from question!
        -> elsewhere
    ?: I won't tell
        Why is that? {
            ?: I do not like you.
            Okay, be that way, punk. <face:weary>
            ?: Just kidding, im good
            -> doingwell
        }
        This should be an inner fallback.

    ?: Can a fallback be a goto?
        I am going mad <face:weary> {
            ?: Let us find out
            Yes, lets
        }
        -> elsewhere
    
    # This question will just jump right to the fallback.
    ?: Question with no response
    
    # Automatically generates a new node with same text as parent of this question.
    ?: Question chain (inherit prompt from parent) {
        ?: Followup question
            Followup answer
        ?: Other followup question
            Followup answer 2
            -> elsewhere
        ?: How deep can we go?
        {
            ?: Oh shit
                Oh shit is right this is terrible
                -> skipelsewhere
            ?: It just keeps going
        }
    }
}
Fallback, we continue here. <face:smile>
Then we keep going.
->skipelsewhere
@elsewhere
We jumped here via goto.
And continue from this tag.
@skipelsewhere
Hopefully my dialogue system loops here <face:weary>
-> start